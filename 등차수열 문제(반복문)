#include <stdio.h>

int main(void) {
  int a, d, n, i, sum;
  //a = 시작값, d= 등비 값,c = 몇 번째인지 나타내는 수
  scanf("%d %d %d", &a, &d, &n);
  sum = a; //시작 값 대입
  
  for(i=1; i<n; i++)
    {
      sum += d;
      //등차 더해서 항 계산
    }
  printf("%d", sum);
  return 0;
}
