#include <stdio.h>


int main(void) {
  int count = 0, i, n;
//count = 자릿수.
  
  scanf("%d", &n);
  
  while(n != 0)
  {
    n /= 10;//나머지가 있으면 반복 없으면 종료.
    ++count;
  }

  printf("%d", count);

  
  return 0;
}
